"""
Inventory reservation tool for Preparation Agent
"""

def reserve_inventory(drug_id: str, quantity: float, reservation_id: str) -> bool:
    """
    Reserve inventory for a prescription (soft lock).
    Args:
        drug_id (str): Drug identifier.
        quantity (float): Quantity to reserve.
        reservation_id (str): Reservation identifier.
    Returns:
        bool: True if reservation successful.
    """
    """
    Simulated inventory reservation for testing.
    """
    return True
